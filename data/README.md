# Dataset

Place the CSV to be analyzed in this folder as `dataset.csv`.

The notebook automatically detects numerical columns and produces summary statistics, histograms, box plots, outlier counts, and a correlation matrix. Do not upload private or restricted data.
